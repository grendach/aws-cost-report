# aws-cost-report
AWS cost report
```bash
python cost_report.py --days 7 --group service --top 5
python cost_report.py --days 30 --group region --profile staging
```
by default, uses `eu-west-1` region
